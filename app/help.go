package app

const HelpText = `generate password cli

Available Commands:
  generate: generate_command generates a password
  list:     list_command gets generated list
  get:      get_command get a generated password
`

const GenerateHelp = `generate password

Usage:
  generate [title] [url]
`

const GetHelp = `get a generated password
The retrieved password is saved to the clipboard.

Usage:
  get [ID]
`
